{
  "name": "mangamaker-workspace",
  "private": true,
  "scripts": {
    "dev": "cd frontend && bun run dev",
    "backend": "cd backend && bun run dev"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  }
}
